# Deferred Supabase client boundary

No client is initialized in the local UI phase. When approved, browser and server client factories will live in this directory only; Features and components must not initialize Supabase clients.
